eval = evolver=function(cI){let t=tested[0];if(!t||!t[0])return setupNNFromParams(T[0][0][100],cI);let s=t[0].slice(1).map((n,i)=>({s:+n,id:i+1})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s);if(!s.length)return setupNNFromParams(T[0][0][100],cI);let maxS=s[0].s, p=s[Math.floor(Math.pow(Math.random(),2)*s.length)];Ds[cI]=JSON.parse(JSON.stringify(t[p.id]));let L=Ds[cI], temp=1.0-Math.pow(p.s/maxS, 4);for(let i=1;i<=L[0][0].numLayers;i++)for(let r=0;r<L[i].length;r++)for(let c=0;c<L[i][r].length;c++){let rnd=Math.random();if(rnd<0.01+0.04*temp)L[i][r][c]=(Math.random()-.5)*4;else if(rnd<0.2+0.2*temp)L[i][r][c]+=(Math.random()-.5)*(Math.random()<.2+0.3*temp?2.0:0.1);}};
