eval = evolver=function(cI){let t=tested[0];if(!t||!t[0])return setupNNFromParams(T[0][0][100],cI);let s=t[0].slice(1).map((n,i)=>({s:+n,id:i+1})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s);if(!s.length)return setupNNFromParams(T[0][0][100],cI);let pIdx=s[Math.floor(Math.pow(Math.random(),2)*s.length)].id;Ds[cI]=JSON.parse(JSON.stringify(t[pIdx]));let L=Ds[cI], g=()=>(Math.random()+Math.random()+Math.random()-1.5)*0.66;for(let i=1;i<=L[0][0].numLayers;i++)for(let r=0;r<L[i].length;r++)for(let c=0;c<L[i][r].length;c++){let rnd=Math.random();if(rnd<.01)L[i][r][c]=g()*4;else if(rnd<.3){let a=g();L[i][r][c]+=Math.random()<.1?a*1.5:a*0.15;}}};
